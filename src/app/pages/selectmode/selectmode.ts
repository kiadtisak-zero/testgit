import { Component } from '@angular/core';
import { Router, RouterLink } from '@angular/router';

@Component({
  selector: 'app-selectmode',
  imports: [RouterLink],
  templateUrl: './selectmode.html',
  styleUrl: './selectmode.css',
})
export class Selectmode {}
